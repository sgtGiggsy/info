document.addEventListener("DOMContentLoaded", fulKivalaszt);

function fulKivalaszt()
{
    changeTitle('beallitastitle', fulszoveg);
    showOnlyOne('beallitas-', kinyit);
    showOnlyOne('sugo-', kinyit);
}